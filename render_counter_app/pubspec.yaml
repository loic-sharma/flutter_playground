name: render_counter_app
description: "A counter app using the rendering layer."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0-142.0.dev

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
